# ============================================================
# MixAuth 本地环境配置模板
#
# 用法：复制本文件为 .env 后按需修改（.env 已被 .gitignore 忽略，不入库）。
# 加载方：build-matrix.bat / run-login-it.bat 启动时自动读取（:load-env）。
# 优先级：已定义的系统/命令行环境变量优先，.env 仅填充缺失项。
# 格式：每行 KEY=VALUE，# 开头为注释；值含空格时用引号包裹。
# ============================================================

# ---- JDK 路径（26.1 需 JDK 25；1.21.x 需 JDK 21）----
# 供 Gradle toolchain 自动检测（gradle.properties 的
# org.gradle.java.installations.fromEnv 读取）与 bat 脚本切换守护进程 JVM。
# 测试子进程的 java/jcmd 由 LctConfig 从 JAVA_HOME 自动解析，无需单独配置。
# 常见安装位置（按需修改为实际路径）：
#   Windows:  C:\Program Files\Java\jdk-25 / C:\Program Files\Eclipse Adoptium\jdk-25.0.1.9
#   macOS:    /Library/Java/JavaVirtualMachines/temurin-25.jdk/Contents/Home
#   Linux:    /usr/lib/jvm/temurin-25-jdk-amd64
JDK25_HOME=C:\Program Files\Java\jdk-25
JDK21_HOME=C:\Program Files\Java\jdk-21

# ---- 登录链集成测试（run-login-it.bat）----
# MCC 无头客户端目录（缺失时在线预检层整体跳过，不报红）
# 下载：https://github.com/MCCTeam/Minecraft-Console-Client/releases
MCC_DIR=C:\tools\Minecraft-Console-Client

# ---- 端口覆盖（可选，默认 25565 / 25575 / 18080）----
# SPORT=25565
# RCON_PORT=25575
# MOCK_PORT=18080